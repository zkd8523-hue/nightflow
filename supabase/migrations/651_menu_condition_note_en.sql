-- ============================================================================
-- Migration 651: 메뉴 조건 문구의 영어판
--
-- 배경(2026-09-06 발견): `condition_note`가 한 컬럼뿐이라 한국어 원문이 외국인
--      메뉴 화면(MenuPicker)에 그대로 나가고 있었다. 실제로 영어로 보는 손님에게
--      "바틀 주문 시 프리패스 & 무료입장", "킵술 오픈 시 인당 10,000원의 비용이
--      발생합니다" 같은 문장이 노출됐다. 1,002개 항목 중 69건.
--
--      이 문구는 그냥 부연이 아니라 **돈과 입장 조건**이다 — 못 읽으면 손님이
--      추가 비용이나 입장 혜택을 모른 채 결제하게 된다.
--
-- name_en/name_ko가 이미 나뉘어 있는 것과 같은 방식으로 en 컬럼을 하나 더 둔다.
-- 비어 있으면 화면에서 그 줄을 접는다(한국어로 폴백하지 않는다 —
-- 못 읽는 문장을 보여주느니 안 보여주는 게 낫다).
-- ============================================================================

ALTER TABLE club_menu_items
  ADD COLUMN IF NOT EXISTS condition_note_en TEXT;

COMMENT ON COLUMN club_menu_items.condition_note IS
  '주문 조건·혜택 안내(한국어 원문). 예: "바틀 주문 시 프리패스 & 무료입장"';
COMMENT ON COLUMN club_menu_items.condition_note_en IS
  '주문 조건·혜택 안내(영어). 외국인 트랙(en/ja/zh/zh-tw)에서 쓴다. 비면 표시하지 않는다.';
