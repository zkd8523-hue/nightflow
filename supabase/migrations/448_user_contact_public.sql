-- ============================================================================
-- Migration 448: 유저 연락처 공개 여부 플래그
-- 날짜: 2026-07-11
-- 설명:
--   일반 유저도 채팅에서 본인 연락처(인스타/오픈채팅/전화)를 카드로 남길 수 있게 확장.
--   등록한 연락처는 기본적으로 비공개(이 대화에서만) — 공개 프로필 노출은 opt-in.
--   contact_public=true 일 때만 공개 프로필에 인스타/오픈채팅 노출.
--   (MD는 md_unique_slug 기준으로 기존대로 노출 — 이 플래그와 무관)
-- ============================================================================
ALTER TABLE users ADD COLUMN IF NOT EXISTS contact_public BOOLEAN NOT NULL DEFAULT false;
