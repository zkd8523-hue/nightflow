-- ============================================================================
-- Migration 655: 한국인 예약 24시간 중복 제한 제거
--
-- Migration 652가 스팸 방지로 (user_id, club_id) 24시간 1건 제한을 걸었는데,
-- 정상적인 사용을 막는 게 더 컸다(2026-09-06 실사용에서 바로 걸림):
--   - 날짜가 다른 예약: 같은 클럽을 금요일·토요일 각각 잡는 건 정상 요청이다.
--   - 취소 후 재신청: 653에서 본인 취소를 열어놨는데, 취소해도 24시간 막히면
--     그 기능이 무의미해진다.
--   - 잘못 넣어서 다시 넣기: 오타 하나 고치려고 하루를 기다릴 수는 없다.
--
-- 로그인 필수라 익명 스팸이 아니고, 운영자가 어드민에서 직접 보고 처리하는
-- 컨시어지 모델이라 DB에서 기계적으로 막을 이유가 없다.
-- ============================================================================

DROP TRIGGER IF EXISTS korean_booking_rate_limit ON korean_booking_requests;
DROP FUNCTION IF EXISTS check_korean_booking_rate_limit();
